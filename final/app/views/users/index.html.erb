<h1>Users of Leal</h1>

<p><a href="/users/new" class="btn btn-primary">New User</a></p>

<table class="table">

	<% users = User.all %>
	<% users.each do |user| %>
	<tr>
	<td>
		<a href="/users/<%= user.id %>"> <%= user.user_name %></a></td>
	</tr>

<% end %>
